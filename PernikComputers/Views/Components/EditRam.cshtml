@model PernikComputers.Models.RamCreateViewModel

@{
    ViewData["Title"] = "Edit";
}

<h1 class="text-center">Edit Ram</h1>
<hr />
<div>
    <form asp-action="EditRam" class="text-center">
        @await Html.PartialAsync("PartialViews/_RamPartialView.cshtml", Model)
        <div class="form-group mt-4">
            <button type="submit" class="btn-radius btn btn-outline-primary btn-lg">EDIT</button>
            <a asp-action="AllTable" asp-controller="Products" class="mt-1 ml-4 d-block">Back to products</a>
        </div>
    </form>
</div>


@section Scripts {
    @{await Html.RenderPartialAsync("_ValidationScriptsPartial");}
}
